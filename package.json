{
  "name": "fireblocks-seismic-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "poc"
  ],
  "scripts": {
    "src20:demo": "bun run poc/src/demo.ts",
    "src20:deploy": "cd contracts && ./script/deploy.sh",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8"
  },
  "dependencies": {
    "prettier": "^3.8.1"
  }
}
